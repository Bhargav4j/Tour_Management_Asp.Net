@page
@model TourManagement.Web.Pages.Tours.IndexModel
@{
    ViewData["Title"] = "Tours";
}

<div class="container">
    <div class="row mb-3">
        <div class="col-md-6">
            <h1>Tours</h1>
        </div>
        <div class="col-md-6 text-end">
            <a asp-page="Create" class="btn btn-primary">Add New Tour</a>
        </div>
    </div>

    @if (!string.IsNullOrEmpty(Model.Message))
    {
        <div class="alert alert-success">@Model.Message</div>
    }

    <div class="row">
        @if (Model.Tours != null && Model.Tours.Any())
        {
            @foreach (var tour in Model.Tours)
            {
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        @if (!string.IsNullOrEmpty(tour.PicturePath))
                        {
                            <img src="~/uploads/@tour.PicturePath" class="card-img-top" alt="@tour.TourName" style="height: 200px; object-fit: cover;" />
                        }
                        else
                        {
                            <div class="card-img-top bg-secondary d-flex align-items-center justify-content-center" style="height: 200px;">
                                <span class="text-white">No Image</span>
                            </div>
                        }
                        <div class="card-body">
                            <h5 class="card-title">@tour.TourName</h5>
                            <p class="card-text">
                                <strong>Place:</strong> @tour.Place<br />
                                <strong>Days:</strong> @tour.Days<br />
                                <strong>Price:</strong> $@tour.Price.ToString("N2")
                            </p>
                            <a asp-page="Details" asp-route-id="@tour.Id" class="btn btn-primary btn-sm">Details</a>
                            <a asp-page="Edit" asp-route-id="@tour.Id" class="btn btn-warning btn-sm">Edit</a>
                            <a asp-page="Delete" asp-route-id="@tour.Id" class="btn btn-danger btn-sm">Delete</a>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <div class="alert alert-info">No tours available. <a asp-page="Create">Create one now</a></div>
            </div>
        }
    </div>
</div>
