@page "{id:int}"
@model TourManagement.Web.Pages.Bookings.DeleteModel
@{
    ViewData["Title"] = "Cancel Booking";
}

<h1>Cancel Booking</h1>

<h3>Are you sure you want to cancel this booking?</h3>

@if (Model.Booking != null)
{
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">@Model.Booking.TourName</h5>

            <dl class="row">
                <dt class="col-sm-3">Place</dt>
                <dd class="col-sm-9">@Model.Booking.Place</dd>

                <dt class="col-sm-3">Booking Date</dt>
                <dd class="col-sm-9">@Model.Booking.BookingDate.ToLocalTime().ToString("MMM dd, yyyy")</dd>
            </dl>

            <form method="post">
                <button type="submit" class="btn btn-danger">Cancel Booking</button>
                <a asp-page="Index" class="btn btn-secondary">Back to List</a>
            </form>
        </div>
    </div>
}
else
{
    <p>Booking not found.</p>
    <a asp-page="Index" class="btn btn-secondary">Back to List</a>
}
